import React from 'react';
import { View } from 'react-native';

// Composant placeholder pour éviter les erreurs d'import
// Vous pourrez réactiver le code AdMob ici une fois les IDs obtenus
export default function GoogleAdBanner() {
    // Retourne une vue vide pour le moment
    return <View />;
}